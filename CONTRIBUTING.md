<!-- codieverse+'s vision, brought to life. -->
# Contributing to SideLab

We love your input! We want to make contributing to this project as easy and transparent as possible.

## Pull Requests
1. Fork the repo and create your branch from `main`.
2. If you've added code that should be tested, add tests.
3. If you've changed APIs, update the documentation.
4. Ensure the test suite passes (`pytest`).
5. Format your code with `black` and `isort`.
6. Make sure your code lints (`ruff`).

## Commit Convention
We follow Conventional Commits format (`feat:`, `fix:`, `chore:`, `docs:`, `refactor:`, `test:`, `perf:`).
